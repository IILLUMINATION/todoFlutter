name: todo_flutter
description: "Приложение пет проект на фреймворке Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  file_picker: ^10.3.3
  hive_flutter: ^1.1.0
  flutter_riverpod: ^3.0.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13  # ИЗМЕНИЛ: понизил версию для совместимости

flutter:
  uses-material-design: true